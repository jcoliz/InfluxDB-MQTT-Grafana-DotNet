version: '3.6'
services:
  sender:
    image: docker.io/jcoliz/mqttsender:local-46c4f6c
    deploy:
      replicas: 3
    restart: always
    environment:
      - MQTT__SERVER=mqtt
    depends_on:
      mqtt:
        condition: service_healthy
  